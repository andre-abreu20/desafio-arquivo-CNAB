# desafio-arquivo-CNAB

Aplicacao feita em Django rest-framework

Ative seu ambiente virtual.

python -m venv venv

source venv/bin/activate

Instale os pacotes:

pip install -r requirements.txt

Rode o servidor localmente

python manage.py runserver
